var Client   = require('../src').Client;
var clearbit = new Client();

clearbit.Risk.calculate({email: 'alex@clearbit.com', ip: '127.0.0.1'})
  .then(function (result) {
    console.log('Result:', result);
  })
  .catch(function (err) {
    console.error(err);
  });

